<%= form_for(job_location) do |f| %>
  <% if job_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job_location.errors.count, "error") %> prohibited this job_location from being saved:</h2>

      <ul>
      <% job_location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :info %>
    <%= f.text_field :info %>
  </div>

  <div class="field">
    <%= f.label :url %>
    <%= f.text_field :url %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
